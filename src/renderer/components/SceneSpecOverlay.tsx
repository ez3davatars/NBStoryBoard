import React from "react"
// Unused import removed

const SceneSpecOverlay: React.FC = () => {
 // Component disabled to prevent red-box bleed-through bug
 return null;
}

export default SceneSpecOverlay

