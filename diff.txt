diff --git a/src/renderer/components/SceneCanvas.tsx b/src/renderer/components/SceneCanvas.tsx
index ad7001f..6b9382e 100644
--- a/src/renderer/components/SceneCanvas.tsx
+++ b/src/renderer/components/SceneCanvas.tsx
@@ -835,33 +835,44 @@ const SceneCanvas = () => {
         for (const r of tokensByDepth) {
             try {
                 const t = r.token;
+                
+                const vw = viewportBox.w > 0 ? viewportBox.w : STAGE_W;
+                const vh = viewportBox.h > 0 ? viewportBox.h : STAGE_H;
+                const scaleX = STAGE_W / vw;
+                const scaleY = STAGE_H / vh;
+                
+                const normX = t.x * scaleX;
+                const normY = t.y * scaleY;
+                const normW = t.width * scaleX;
+                const normH = t.height * scaleY;
+
                 const img = await loadDataUrlImage(t.url);
 
-                const ax = (t.anchorX ?? 0.5) * t.width;
-                const ay = (t.anchorY ?? 0.8) * t.height;
+                const ax = t.anchorX ?? 0.5;
+                const ay = t.anchorY ?? 0.8;
 
                 const imgRatio = img.width / img.height;
-                const boxRatio = t.width / t.height;
-                let drawW = t.width;
-                let drawH = t.height;
+                const boxRatio = normW / normH;
+                let drawW = normW;
+                let drawH = normH;
                 let offX = 0;
                 let offY = 0;
 
                 if (imgRatio > boxRatio) {
-                    drawW = t.width;
-                    drawH = t.width / imgRatio;
-                    offY = (t.height - drawH) / 2;
+                    drawW = normW;
+                    drawH = normW / imgRatio;
+                    offY = (normH - drawH) / 2;
                 } else {
-                    drawH = t.height;
-                    drawW = t.height * imgRatio;
-                    offX = (t.width - drawW) / 2;
+                    drawH = normH;
+                    drawW = normH * imgRatio;
+                    offX = (normW - drawW) / 2;
                 }
 
                 ctx.save();
-                ctx.translate(t.x + ax, t.y + ay);
+                ctx.translate(normX, normY);
                 ctx.rotate((t.rotation * Math.PI) / 180);
                 ctx.scale(t.scaleX, t.scaleY || 1);
-                ctx.drawImage(img, -ax + offX, -ay + offY, drawW, drawH);
+                ctx.drawImage(img, (-normW * ax) + offX, (-normH * ay) + offY, drawW, drawH);
                 ctx.restore();
             } catch (e) {
                 console.warn("Failed to draw token on anchor plate", r.token.id, e);
@@ -935,7 +946,8 @@ const SceneCanvas = () => {
                     state.annotations, 
                     state.referenceSlots, 
                     state.director,
-                    extractedStyle
+                    extractedStyle,
+                    viewportBox
                 );
 
                 const refs: { url: string; label: string }[] = [];
@@ -3116,8 +3128,22 @@ const SceneCanvas = () => {
                         </div>
 
                         {/* RENDER ACTIONS */}
-                        <div className="flex gap-2">
-
+                        <div className="flex gap-2 items-center">
+                            {viewMode === 'result' && (state.resultImage || (activeShot && activeShot.latestCompositeResultUrl)) && (
+                                <button
+                                    onClick={() => {
+                                        const url = state.resultImage || activeShot?.latestCompositeResultUrl;
+                                        if (url) {
+                                            dispatch({ type: 'SET_BG', payload: url });
+                                            setViewMode('stage');
+                                            dispatch({ type: 'ADD_LOG', payload: { message: "Result promoted to Stage Background.", type: 'success' } });
+                                        }
+                                    }}
+                                    className="px-6 py-2 h-full bg-[#09090b] hover:bg-green-950/40 border border-green-500/30 hover:border-green-500/80 rounded-lg text-[10px] font-bold tracking-widest text-green-400 uppercase transition-all shadow-[0_0_10px_rgba(34,197,94,0.05)] hover:shadow-[0_0_15px_rgba(34,197,94,0.2)]"
+                                >
+                                    Use as Stage Scene
+                                </button>
+                            )}
                             <button
                                 onClick={generateBg}
                                 disabled={state.isProcessing}
diff --git a/src/renderer/utils/promptHelpers.ts b/src/renderer/utils/promptHelpers.ts
index 0a7ba37..05f3883 100644
--- a/src/renderer/utils/promptHelpers.ts
+++ b/src/renderer/utils/promptHelpers.ts
@@ -359,7 +359,8 @@ export const buildStrictPrompt = (
     annotations: StageAnnotation[],
     referenceSlots: ReferenceSlot[],
     director: DirectorSettings,
-    extractedStyle?: ExtractedStyle | null
+    extractedStyle?: ExtractedStyle | null,
+    viewportBox?: { w: number, h: number }
 ) => {
     const tech = buildMasterStyleKeywords(director);
 
@@ -371,12 +372,40 @@ export const buildStrictPrompt = (
 
     const regions = plan.map(r => {
         const t = r.token;
-
-        const boundsBlock = `BBOX_ABS: [${Math.round(t.x)}, ${Math.round(t.y)}, ${Math.round(t.width)}, ${Math.round(t.height)}]`;
-        let profile = typeof r.profile === 'string' ? r.profile : (r.profile ? JSON.stringify(r.profile) : `You MUST perfectly match the facial identity, skin tone, hair, and clothing of the subject in the attached image labeled "REGION_${r.region}_REF"`);
+        const vw = viewportBox?.w || STAGE_W;
+        const vh = viewportBox?.h || STAGE_H;
+        const scaleX = 1000 / vw;
+        const scaleY = 1000 / vh;
+        const ax = t.anchorX ?? 0.5;
+        const ay = t.anchorY ?? 0.8;
+        const topLeftX = t.x - (t.width * ax);
+        const topLeftY = t.y - (t.height * ay);
+        const botRightX = topLeftX + t.width;
+        const botRightY = topLeftY + t.height;
+
+        const xmin = Math.floor(topLeftX * scaleX);
+        const ymin = Math.floor(topLeftY * scaleY);
+        const xmax = Math.ceil(botRightX * scaleX);
+        const ymax = Math.ceil(botRightY * scaleY);
+        const boundsBlock = `BBOX: [${Math.min(1000, Math.max(0, ymin))}, ${Math.min(1000, Math.max(0, xmin))}, ${Math.min(1000, Math.max(0, ymax))}, ${Math.min(1000, Math.max(0, xmax))}]`;
+        
+        let profileDesc = "";
+        if (typeof r.profile === 'string') {
+            profileDesc = r.profile;
+        } else if (r.profile && typeof r.profile === 'object') {
+            const parts = [];
+            if (r.profile.identity) parts.push(`Subject: ${r.profile.identity}`);
+            if (r.profile.wardrobe) parts.push(`Wardrobe: ${r.profile.wardrobe}`);
+            if (r.profile.accessories) parts.push(`Accessories: ${r.profile.accessories}`);
+            if (r.profile.style) parts.push(`Style: ${r.profile.style}`);
+            profileDesc = parts.join(', ');
+        }
+
+        let profile = `You MUST perfectly match the facial identity, skin tone, hair, and clothing of the subject in the attached image labeled "REGION_${r.region}_REF".`;
+        if (profileDesc) profile += ` Supplementary descriptors: [${profileDesc}]`;
         if (t.intelligence) profile += `\nMANDATORY ACTION/POSE: ${t.intelligence}`;
 
-        return `REGION ${r.region} (${r.actorLabel}):\n- Position: ${boundsBlock}\n- Description: ${profile}`;
+        return `REGION ${r.region} (${r.actorLabel || t.tag}):\n- Position: ${boundsBlock}\n- Description: ${profile}`;
     }).join('\n\n');
 
     // Director Canvas Semantic Handoff
@@ -404,8 +433,9 @@ export const buildStrictPrompt = (
         "- NEGATIVE SPACE: Ignore any solid or white studio backgrounds present in the REGION_REFS. Treat flat white areas (such as inside a hollow helmet, or between arms and torso) as transparent, and fill them perfectly with the scene environment.",
         "- NO OUTLINES: Do NOT draw any boxes, boundaries, or outlines around the characters. The final image must look like a natural photograph or movie frame.",
         "- NO Hallucinations: Do not add any extra objects, people, or details not requested in the Director Brief or Region Plan.",
-        "- ASPECT RATIO LOCK: DO NOT STRETCH OR SQUASH. If a character cutout does not perfectly fill its assigned BBOX_ABS, DO NOT distort the character. Maintain natural proportions and fill any remainder with pixels from the CLEAN_BG_PLATE.",
-        "- OVERLAP LOCK: If the ANCHOR_GUIDE shows subjects overlapping, maintain that exact occlusion.",
+        "- SCALE LOCK & PROPORTION: The actor MUST perfectly match the exact physical scale and proportion of the cutout shown in the ANCHOR_GUIDE natively. Do NOT expand or balloon the actor to fill the empty BBOX space if the visual anchor mask is smaller. The BBOX coordinates specify the absolute maximum physical limits.",
+        "- OCCLUSION & SCENE PHYSICS: If the Anchor Guide physically shows an actor's body (e.g. legs) awkwardly painted ON TOP of foreground furniture (like a solid desk or table), but the environment layout or intent dictates they should be sitting or standing BEHIND it, you MUST selectively occlude and hide those lower body parts behind the furniture. Prioritize natural depth and scene physics over tracing the exact anchor edge.",
+        "- OVERLAP LOCK: If the ANCHOR_GUIDE shows multiple human subjects overlapping each other, maintain that exact character-to-character occlusion constraint.",
         "",
         dnaBlock ? `### ANCHOR DNA:\n${dnaBlock}\n` : "",
         notes ? `### DIRECTOR NOTES (EXPLICIT USER REQUEST - MANDATORY LOCATION/SCENE):\n${notes}\n` : "",
